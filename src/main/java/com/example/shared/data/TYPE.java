package com.example.shared.data;

/**
 * Enum defining the types of packages for network communication.
 */
public enum TYPE
{
    LOGIN,
    SEND_EMAIL,
    REQUEST_INBOX,
    DELETE_EMAIL,
    ANSWER,
    FORWARD
}
